exports.drink = function(stock, number) {
  stock.units -= number;
  return number + " " + stock.name + " drunk";
};

exports.buy = function(stock, number) {
  stock.units += number;
  return number + " " + stock.name + " bought";
};
